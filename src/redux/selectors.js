export const allNotices = state => state.notices.items;

export const noticeById = state => state.notices.notice;

export const isLoading = state=> state.notices.isLoading;

export const favorite = state => state.notices.favorite;